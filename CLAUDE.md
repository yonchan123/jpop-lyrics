# jpop-lyrics — 리포지토리 규약

일본 곡 가사를 한국어로 옮긴 번역 모음. 새 번역 작업은 아래 규약을 **그대로** 따를 것.
번역 파이프라인 자체(가사 소스 탐색·추출·번역 규칙)는 `ja-lyrics-3line` 스킬을 사용한다.
이 문서는 스킬보다 **우선**한다 — 충돌하면 이 문서가 이긴다.

---

## 1. 디렉터리 구조

```
README.md                     ← 루트 인덱스 (컬렉션 목록)
translation/
  {컬렉션 이름}/
    NN. {곡 제목} - {아티스트}.md
    README.md                 ← 곡 목록 테이블
    _REPORT.md                ← 수집·검수 상태 리포트
```

### 컬렉션 종류

| 종류 | 이름 규칙 | 정렬 |
|---|---|---|
| 전곡 컬렉션 | `{아티스트} - ALL` | **발매일 오름차순** |
| 앨범 | `{아티스트} - {앨범명}` | 앨범 수록 순 |
| 공연 셋리스트 | `{공연명}` | 공연 진행 순 |

- 아티스트의 전곡을 모으는 컬렉션은 **반드시 폴더명에 `- ALL`을 붙인다.**
- `- ALL` 폴더의 개별 앨범 폴더는 중복이므로 두지 않는다. 단, 셋리스트 폴더는 ALL에 없는 곡(콜라보·타 밴드 커버 등)을 담을 수 있으므로 함부로 지우지 않는다 — 지우기 전 전곡이 ALL에 있는지 대조할 것.

---

## 2. 곡 파일 포맷

```markdown
# {곡 제목}

**{아티스트}**

---

**作词 : {작사가}**␣␣
작곡 : {작곡가}

**{일본어 원문}**␣␣
{한글 음차}␣␣
{한국어 의미}

**{다음 줄 원문}**␣␣
{한글 음차}␣␣
{한국어 의미}
```

- `␣␣` = 후행 공백 2칸 (마크다운 하드브레이크). **원문 줄과 음차 줄은 반드시** 2칸으로 끝나고, **의미 줄은 후행 공백 없음**.
- 블록 사이는 빈 줄 1개. 원문의 절 구분 빈 줄은 그대로 보존.
- 크레딧 표기는 **간체 `作词` + 한국어 `작곡`** 이 하우스 스타일이다 (`作詞`/`作曲` 아님). 편곡이 있으면 셋째 줄에 `편곡 : ...`.
- **커버곡은 원곡자 크레딧을 그대로 쓴다.** "(cover)" 같은 표기는 붙이지 않는다.
- 후행 공백 4칸을 쓰는 폴더도 있다 — 파일을 고칠 때는 **그 파일의 기존 관례를 따른다**.

### 가사를 못 구한 경우

```
(lyrics not available)
```

연주곡이면:

```
(instrumental — 가사 없는 연주곡)
```

---

## 3. 음차(읽기) 표기 규약

리포 전역에서 통일한다. 새 파일은 물론, 기존 파일을 수정할 때도 이 기준.

| 항목 | 규칙 | 예 |
|---|---|---|
| 장음 | **모음을 펼쳐 적는다** (대시 금지) | おう/おお → 오오, ゆう → 유우, えい → 에이, ヒーロー → 히이로오 |
| 촉음 っ | 앞 음절 **ㅅ 받침** | まって → 맛테, ずっと → 즛토 |
| 촉음이 앞 음절 없이 올 때 | 인용부호·기호 뒤에서는 **떨어뜨린다** | 「…」って → 「…」테 |
| ん | 뒤 자음에 따라 ㄴ/ㅇ 받침, 단독이면 은 | しんぱい → 신파이 |
| じゃ/じゅ/じょ | **쟈/쥬/죠** | じゃない → 쟈나이 |
| ざ/ず/ぞ | 자/즈/조 | ざんねん → 잔넨 |
| 가타카나 외래어 | **일본어 발음** (한국식 외래어 표기 금지) | ラブ → 라부(×러브), タイム → 타이무(×타임), ハート → 하아토(×하트) |
| 로마자 단어 | 음차 줄에 **원형 그대로** 유지 | `Sky에 …`, `WINNER니 나루` |

**금지 사항**
- 고립 자모(ㅅ, ㄴ 등)를 단독으로 남기지 말 것 — 반드시 받침으로 합성.
- 음차 줄에 가나(ん, っ, ノ, ざ …)를 남기지 말 것.
- 장음 표기로 `ㅡ`/하이픈을 쓰지 말 것.
- 단, 원문이 `oh-oh-oh` 처럼 하이픈을 **구분자**로 쓰면 음차에서도 구분자를 유지한다 (장음으로 뭉치지 말 것).

---

## 4. 폴더 README (곡 목록)

헤더·설명문·Spotify 링크는 유지하고, 곡 목록은 **테이블**로 적는다.

```markdown
| # | 발매일 | 곡 | 가수 | 읽기 | 뜻 |
|---:|---|---|---|---|---|
| 1 | 2020-10-23 | [うっせぇわ](01.%20...md) | Ado | 웃세에와 | 시끄러워 |
```

- `#` 는 파일명 번호와 **항상 일치**해야 한다.
- `발매일`은 **최초 발매일** `YYYY-MM-DD`. 선행 배신이 CD보다 빠르면 배신일을 쓴다. 정말 모를 때만 `발매일 미상`.
- `곡` 셀은 제목만 (아티스트 제외), 링크 대상은 **퍼센트 인코딩된 파일명**.
- `읽기`/`뜻`은 **곡 제목**의 한글 음차와 한국어 뜻 (가사가 아님). §3 음차 규약을 그대로 적용.
- 셀 안의 `|` 는 `\|` 로 이스케이프.

---

## 5. 발매순 정렬 (`- ALL` 폴더)

`- ALL` 컬렉션은 **발매일 오름차순**을 유지한다.

- 같은 날 발매(동일 싱글/앨범 수록)면 기존 상대 순서를 보존한다 (안정 정렬).
- 곡을 추가하면 정렬이 깨지므로, 추가 후 **파일명 번호를 다시 매기고** README 테이블과 `_REPORT.md` 번호를 함께 갱신한다.
- 파일 리네임은 **2단계**로 (임시 이름 → 최종 이름) 충돌을 피한다.
- 앨범·셋리스트 폴더는 정렬하지 않는다 (수록 순/공연 순이 의도).

---

## 6. `_REPORT.md`

수집·검수 이력. 상태 테이블의 번호는 README·파일명과 **동기화**되어야 한다.

```markdown
총 {N}곡 — 전부 ok

| # | Song | Status |
|---|------|--------|
| 01 | ... | ok |
```

- 상태값: `ok` / `ok_with_low_conf` / `flagged` / `no_lyrics` / `instrumental`
- 곡을 추가하면 헤더의 총 곡수와 테이블 행을 함께 갱신.
- 문제를 해결했으면 상태를 `ok`로 되돌리고 "해결 이력"에 판단 근거를 남긴다.

---

## 7. 루트 README

컬렉션 인덱스. 번호는 연속, 링크는 퍼센트 인코딩.

```markdown
1. [{컬렉션}](translation/{인코딩된%20폴더명}/) — {N} tracks ({설명})
```

컬렉션을 추가·삭제·개명하면 이 인덱스의 링크·곡 수·번호를 함께 고친다.

---

## 8. 가사 소스

`ja-lyrics-3line` 스킬의 Stage 3을 따르되, 이 리포의 실측 결과를 반영한다.

- **가사 본문은 `curl`로 받는다.** `WebFetch`는 가사 전문을 거부하므로 쓰지 않는다 (메타데이터 조회에는 써도 됨).
- 우선순위: `utaten.com` (`div.hiragana`, ruby 태그 제거 — 후리가나가 있어 독음 확인에 유리) → `j-lyric.net` → `kashinavi.com` → `petitlyrics.com`
- **`uta-net.com`은 Cloudflare 403으로 curl이 막힌다.** 검색 결과 확인용으로만 쓰고 본문 추출은 위 사이트로.
- 아주 최신 곡은 어느 사이트에도 없을 수 있다. 3후보 + 폴백 1회까지 시도하고 안 되면 `no_lyrics` 스텁으로 남긴다.

---

## 9. 배포

원격은 **최신 스냅샷 1커밋만** 유지한다 (히스토리 없음).

```bash
git add -A
git checkout --orphan snapshot-latest -q
git commit -q -m "Latest snapshot: J-pop/J-rock lyric translations (JA→KO 3-line)"
git branch -D main -q && git branch -m main

# 토큰은 ~/.netrc 에서 읽어 URL에 실어 보내고, 출력은 반드시 마스킹
T=$(awk 'BEGIN{IGNORECASE=1} /machine/{c=""} /login/{c=$2} /password/{if(c=="yonchan123"){print $2; exit}}' ~/.netrc)
git -c credential.helper= push -f "https://yonchan123:${T}@github.com/yonchan123/jpop-lyrics.git" main 2>&1 | sed -E "s/$T/***/g"
T=$(awk 'BEGIN{IGNORECASE=1} /machine/{c=""} /login/{c=$2} /password/{if(c=="changyeon"){print $2; exit}}' ~/.netrc)
git -c credential.helper= push -f "https://changyeon:${T}@github.com/changyeon/jpop-lyrics.git" main 2>&1 | sed -E "s/$T/***/g"
unset T
```

- 대상 저장소 **둘 다**: `yonchan123/jpop-lyrics` (public), `changyeon/jpop-lyrics` (private)
- force push는 원격 히스토리를 덮어쓰므로, 사용자 지시 없이 임의로 하지 말 것.
- 토큰을 절대 출력하지 말 것. 로컬 `.netrc` 토큰이 만료됐으면 `ssh george` 쪽 `~/.netrc`에 유효 토큰이 있다.

---

## 10. 작업 후 검증

파일을 많이 고쳤으면 배포 전에 스크립트로 전수 확인한다.

- 하드브레이크: 같은 블록 안에서 후행 공백 2칸 없이 다음 줄이 이어지는 곳이 없는지
- 음차 줄: 가나 잔존 0, 고립 자모 0
- README 테이블: 행 수 = 파일 수, `#` = 파일명 번호, 링크 디코드 결과가 실제 파일과 일치, 빈 셀 0
- `- ALL` 폴더: 발매일 역전 0, 번호가 1..N 연속
- `_REPORT.md`: 총 곡수·행 번호가 README와 일치

**대량 치환 시 주의** — 실제로 겪은 사고 두 가지:
1. 치환 대상이 결과 문자열의 **접두사**면 이중 변환된다 (`메모리` → `메모리이` 적용 시 이미 맞던 `메모리이`가 `메모리이이`로). 치환 전 결과형 존재 여부를 확인할 것.
2. 하이픈을 일괄로 장음 처리하면 **구분자 하이픈**까지 뭉갠다. 원문 맥락을 보고 판단할 것.

---

## 11. 서브에이전트 운영

곡 수가 많으면 병렬 처리하되:

- 같은 파일을 **동시에 편집시키지 말 것**. 전체 덮어쓰기(Write)는 동시 편집 시 상대 수정을 날린다 — 공유 파일은 targeted Edit을 쓰게 한다.
- 에이전트에는 **범위·포맷·규약을 명시**한다. 소표본만 보고 하우스 스타일을 "통일"하려는 오판이 실제로 있었다 (크레딧 `作词`를 `作詞`로 바꾸는 등). 리포 전역 통계로 관례를 확인한 뒤 고칠 것.
- 결과 보고는 `SendMessage`로 받아야 한다. 최종 응답만 쓰면 전달되지 않으므로, 유휴 전환됐는데 보고가 없으면 직접 요청한다.
- 보고 내용을 그대로 믿지 말고 수치는 직접 재검증한다.
